{
  "name": "@frontcore/config",
  "version": "0.1.0",
  "private": true,
  "description": "FrontCore — configuração e tsconfig partilhados (domain-agnostic).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "tsconfig.base.json"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3"
  }
}
