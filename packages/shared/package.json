{
  "name": "@frontcore/shared",
  "version": "0.1.0",
  "private": true,
  "description": "FrontCore — tipos e utilitários genéricos (domain-agnostic).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
