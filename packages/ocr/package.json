{
  "name": "@frontcore/ocr",
  "version": "0.1.0",
  "private": true,
  "description": "FrontCore — motor de OCR extensível (contrato + providers), reutilizável por qualquer produto.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "tesseract.js": "^7.0.0",
    "@frontcore/config": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
