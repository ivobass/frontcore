{
  "name": "@frontcore/notifications",
  "version": "0.1.0",
  "private": true,
  "description": "FrontCore — contrato de notificações genérico (impl real em fase futura).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
