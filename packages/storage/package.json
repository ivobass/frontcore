{
  "name": "@frontcore/storage",
  "version": "0.1.0",
  "private": true,
  "description": "FrontCore — storage de objetos S3-compatível (MinIO/S3), reutilizável por qualquer produto.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/s3-request-presigner": "^3.600.0",
    "@frontcore/config": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
